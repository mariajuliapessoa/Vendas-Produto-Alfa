"""
Treinamento de modelo (placeholder)
Inclui baselines (Prophet/SARIMAX) e ML (LightGBM).
"""

def train_model(X, y, model_type="lightgbm"):
    print(f"Placeholder: treinar modelo {model_type}")
    return None

if __name__ == "__main__":
    X, y = None, None  # placeholders
    model = train_model(X, y)
